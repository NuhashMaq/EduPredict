## Database
# Local dev default (no Docker required):
DATABASE_URL=sqlite+aiosqlite:///./edupredict.db
# Optional (Postgres):
# DATABASE_URL=postgresql+asyncpg://edupredict:edupredict@localhost:5432/edupredict

## Security
JWT_SECRET=REPLACE_ME_WITH_A_LONG_RANDOM_SECRET
JWT_ISSUER=edupredict
JWT_AUDIENCE=edupredict-web
ACCESS_TOKEN_EXPIRE_MINUTES=30

## Runtime
APP_ENV=development
APP_DEBUG=false
APP_LOG_LEVEL=INFO

# Comma-separated list. Include 3001 because Next.js will auto-bump ports if 3000 is busy.
CORS_ALLOW_ORIGINS=http://127.0.0.1:3000,http://localhost:3000,http://127.0.0.1:3001,http://localhost:3001
# Trusted host names for Host header validation (set real domains in production).
ALLOWED_HOSTS=localhost,127.0.0.1

# Admin bootstrap (disabled by default)
ALLOW_ADMIN_BOOTSTRAP=false
ADMIN_BOOTSTRAP_TOKEN=

# ML registry (where versioned model artifacts are stored)
MODEL_REGISTRY_PATH=ml_registry
